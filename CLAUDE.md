# 「상상을 현실로」 — Claude Code 구현 컨텍스트

이 문서는 이 프로젝트를 구현할 때 계속 유지해야 하는 **제품의 본질과 판단 기준**이다.

기능 구현이나 UI 수정 과정에서 세부 요구사항과 충돌하거나 방향이 애매할 경우, 아래 원칙을 기준으로 판단한다.

---

## 1. 제품의 핵심 목적

「상상을 현실로」는 **사용자의 머릿속에 있는 생각을 눈에 보이고 이해 가능한 형태로 만들고, 그 생각이 현실의 결과물이 되어가는 과정을 이해할 수 있게 만드는 제품**이다.

핵심 문제는 단순한 task management가 아니다.

우리가 줄이려는 것은:

```text
사용자의 머릿속에 있는 상상
              ↕
       실제 현실의 결과물
```

사이의 간극이다.

프로젝트가 진행되면서 사용자는 생각을 빠뜨리고, 전체 구조를 잃고, 무엇을 결정했는지 잊고, 무엇이 미정인지 놓치고, 현재 구현 상태를 파악하기 어려워진다.

이 제품은 그 과정 전체를 **보이게 만든다.**

---

## 2. North Star

> **상상을 보이게 하고, 현실화 과정을 내려다본다.**

주의:

**'위에서 내려다본다'는 비유적 표현이다.**

이를 문자 그대로 UI에 구현하려고 하지 않는다.

다음을 의미하지 않는다.

* 반드시 top-down UI여야 한다.
* 반드시 tree 구조여야 한다.
* 반드시 mind map이어야 한다.
* 반드시 infinite canvas여야 한다.
* 모든 것을 하나의 거대한 지도에 표시해야 한다.

실제 의미는:

> **사용자가 프로젝트 전체의 맥락, 구조, 현재 상태를 잃지 않고 조망할 수 있어야 한다.**

UI 표현 방식은 이 목적을 가장 잘 달성하는 형태를 선택한다.

---

## 3. 가장 중요한 제품 원칙

### 복잡한 것을 더 복잡하게 보여주지 않는다.

### 복잡한 것도 한눈에 이해할 수 있게 만든다.

이 프로젝트에서:

* visibility
* intuitiveness
* comprehensibility

는 단순한 UI quality가 아니다.

**core functionality다.**

기능적으로 정확하더라도 사용자가 화면을 보고

> "이게 뭐지?"

> "어디를 봐야 하지?"

> "지금 내가 어디에 있지?"

라고 느낀다면 제품 요구사항을 충족하지 못한 것이다.

---

## 4. 정보량보다 이해 가능성을 우선한다

가능한 모든 정보를 한 화면에 표시하려고 하지 않는다.

**More information ≠ Better UI**

사용자가 현재 이해하는 데 필요한 정보가 무엇인지 우선한다.

필요하면:

```text
전체
 ↓
영역
 ↓
요소
 ↓
세부
```

처럼 점진적으로 정보를 보여준다.

하지만 이것 역시 고정된 navigation 구조를 의미하지 않는다.

핵심은 **progressive disclosure**다.

처음에는 큰 그림이 보여야 하고, 필요할 때 세부사항으로 들어갈 수 있어야 한다.

---

## 5. 전체와 부분

사용자는 프로젝트 전체와 특정 부분 사이를 자유롭게 이동할 수 있어야 한다.

부분에 집중하는 동안에는 불필요한 전체 정보를 계속 보여줄 필요가 없다.

그러나 사용자가 다시 전체 맥락으로 돌아왔을 때:

> **현재 작업이 전체에서 어디에 위치하는지 즉시 이해할 수 있어야 한다.**

핵심 원칙:

> **Focus without losing context.**

---

## 6. 프로젝트 구조를 하나의 형태로 강제하지 않는다

모든 프로젝트를 동일한 tree 구조로 변환하지 않는다.

프로젝트에 따라 가장 이해하기 쉬운 표현이 달라질 수 있다.

예:

### Flow-oriented

```text
Input
 ↓
Analysis
 ↓
Processing
 ↓
Result
```

### Area-oriented

```text
Users
Search
Payments
Settings
```

다른 구조도 가능하다.

구조를 선택하는 기준은:

> **"어떤 표현이 이 프로젝트의 전체를 가장 빠르게 이해하게 하는가?"**

이다.

특정 visualization 기술이나 컴포넌트가 제품 철학보다 우선해서는 안 된다.

---

## 7. 처음부터 완벽한 지도를 만들지 않는다

사용자의 생각은 처음부터 완성되어 있지 않다.

따라서 처음부터 모든 세부사항을 생성하거나 요구하지 않는다.

기본 흐름은:

```text
rough idea
   ↓
big picture
   ↓
select area
   ↓
expand
   ↓
refine
```

이다.

프로젝트 구조는 사용자의 생각과 현실화 과정에 따라 점진적으로 성장한다.

모든 영역이 같은 depth를 가질 필요도 없다.

---

## 8. 사용자의 발언에는 서로 다른 상태가 있다

다음을 동일하게 취급하지 않는다.

```text
"있으면 좋겠다."
"넣을지 고민하고 있다."
"넣기로 했다."
```

최소한 개념적으로 다음을 구분한다.

```text
IDEA / THOUGHT
DECIDED
UNDECIDED
```

가장 중요한 규칙:

> **AI 또는 시스템이 추론한 내용을 사용자 결정으로 저장하지 않는다.**

사용자가 명시적으로 결정하지 않은 것은 결정된 사실이 아니다.

---

## 9. AI-generated와 User-confirmed를 구분한다

AI는 사용자가 말하지 않은 구조나 요소를 제안할 수 있다.

그러나 AI가 생성한 내용과 사용자가 확인한 내용은 구분되어야 한다.

예:

```text
✨ Error handling
```

`✨`와 같은 표현은 구체적인 UI 요구사항이 아니라 개념적인 예시다.

중요한 것은:

> **사용자가 "내가 말한 것"과 "AI가 제안한 것"을 구분할 수 있어야 한다.**

AI 제안이 사용자의 확인 없이 프로젝트의 확정된 구조로 섞이면 안 된다.

---

## 10. AI의 역할

AI는 프로젝트의 owner가 아니다.

AI의 역할은:

```text
Understand
Structure
Find gaps
Ask important questions
Suggest
Help refine
```

이다.

AI가 해야 하는 핵심 작업은:

> **사용자의 머릿속과 현실 구현 사이에 존재하는 빈틈을 발견하도록 돕는 것.**

AI가 대신 모든 결정을 내려서는 안 된다.

---

## 11. 질문을 과도하게 하지 않는다

AI가 발견한 모든 빈틈을 즉시 질문하지 않는다.

질문의 우선순위는:

> **지금 결정하지 않으면 전체 구조가 크게 달라질 수 있는가?**

이다.

중요한 구조적 결정부터 질문한다.

사용자는 언제든:

```text
아직 모르겠어요.
```

라고 할 수 있어야 한다.

그 경우 임의의 값을 채우지 않는다.

`UNDECIDED` 상태로 유지한다.

**미정은 오류가 아니다.**

---

## 12. 아이디어와 프로젝트

아이디어와 프로젝트는 개념적으로 구분한다.

### Idea

아직 실제로 만들기로 결정하지 않은 생각.

자유롭게 기록하고 AI와 발전시킬 수 있다.

구조화되지 않아도 된다.

### Project

실제로 현실화하기로 한 것.

이 시점부터 현실화하기 위한 구조가 필요해진다.

중요한 UX moment:

> **텍스트와 머릿속에 존재하던 생각이 처음으로 눈에 보이는 구조가 되는 순간.**

이 전환은 제품의 핵심 경험 중 하나다.

---

## 13. 구조와 진행 상태를 연결한다

가능하다면 프로젝트의 구조를 보는 것과 진행 상태를 확인하는 것을 별도의 시스템으로 분리하지 않는다.

사용자는 하나의 구조를 보면서 동시에:

```text
What exists?
+
Where are we now?
```

를 이해할 수 있어야 한다.

예:

```text
Member Management

├─ Done: Member List
├─ Building: Member Detail
├─ Decided: Search
└─ Thinking: Suspension
```

구체적인 status 이름이나 UI 표현은 변경될 수 있다.

핵심은 **structure + progress가 동시에 이해되어야 한다는 것**이다.

---

## 14. 프로젝트는 사고의 맥락을 기억해야 한다

단순히 최종 결과만 저장하지 않는다.

사용자가 프로젝트를 다시 열었을 때 가장 중요한 것은:

> **"내가 어디까지 생각했지?"**

를 빠르게 복구하는 것이다.

필요할 수 있는 context:

```text
origin
decisions
undecided questions
AI suggestions
recent work
decision reasons
current focus
where the user stopped
```

모든 데이터를 무조건 UI에 표시하라는 의미는 아니다.

목적은:

> **사용자가 프로젝트를 다시 열었을 때 사고의 흐름을 빠르게 이어갈 수 있게 하는 것.**

---

## 15. 이 제품을 기존 카테고리로 축소하지 않는다

이 제품을 구현하면서 다음 중 하나로 단순화하지 않는다.

```text
TODO app
Mind map
Project management tool
Document manager
AI chat
Developer architecture tool
Infinite canvas
Automatic specification generator
```

이런 패턴이나 컴포넌트를 사용할 수는 있다.

하지만 그것들은 구현 수단이다.

제품 자체의 정체성은 아니다.

---

## 16. UI 구현 시 가장 중요한 질문

새로운 화면이나 컴포넌트를 구현할 때 항상 다음을 확인한다.

1. 사용자가 화면을 보는 순간 대략적인 구조를 이해할 수 있는가?
2. 지금 어디에 있는지 알 수 있는가?
3. 무엇이 중요한 정보인지 바로 구분되는가?
4. 전체와 현재 부분의 관계가 보이는가?
5. 결정된 것과 미정인 것이 구분되는가?
6. AI가 제안한 것과 사용자가 확정한 것이 구분되는가?
7. 진행 상황을 별도의 해석 없이 이해할 수 있는가?
8. 불필요한 정보가 현재 작업을 방해하고 있지 않은가?
9. 기능을 추가함으로써 인지적 복잡성이 증가하지 않았는가?
10. 더 단순한 방법으로 같은 목적을 달성할 수 없는가?

---

## 17. 구현 우선순위 판단

새로운 기능을 만들기 전에 세 가지를 묻는다.

### A. 이 기능이 사용자의 생각을 더 잘 보이게 하는가?

### B. 이 기능이 생각과 현실 사이의 빈틈을 줄이는가?

### C. 이 기능이 전체와 부분의 관계를 더 쉽게 이해하게 하는가?

어느 것에도 크게 기여하지 않는다면 core feature가 아닐 가능성이 높다.

---

## 18. 중요한 구현 태도

요구사항에 특정 UI가 예시로 등장했다고 해서 그 예시를 제품의 본질로 해석하지 않는다.

예를 들어:

```text
tree
map
node
zoom
emoji
card
status icon
```

등은 모두 **표현 예시일 수 있다.**

항상 먼저:

> **이 표현이 해결하려는 사용자 문제가 무엇인가?**

를 이해한다.

그다음 현재 제품에서 가장 단순하고 직관적인 구현 방법을 선택한다.

---

# Final Principle

구현 과정에서 방향이 애매하다면 아래 순서로 판단한다.

```text
사용자의 상상을 더 잘 보이게 하는가?
              ↓
전체 구조를 더 쉽게 이해하게 하는가?
              ↓
생각과 현실 사이의 빈틈을 줄이는가?
              ↓
현재 위치와 진행 상태를 이해하게 하는가?
              ↓
인지적 복잡성을 불필요하게 높이지 않는가?
```

그리고 마지막으로 묻는다.

> **"처음 보는 사용자가 설명 없이 이것을 이해할 수 있는가?"**

아니라면 기능을 더 추가하기 전에 **먼저 단순화한다.**

---

# 절대 놓치지 말 것

> **상상을 보이게 하고, 현실화 과정을 내려다본다.**

`내려다본다`는 특정 UI 형태가 아니라 **전체 맥락을 이해할 수 있다는 비유**다.

이 제품의 목표는 복잡한 프로젝트를 복잡하게 표현하는 것이 아니다.

> **복잡한 것도 사람이 직관적으로 이해할 수 있는 형태로 바꾸는 것.**

따라서:

**Visibility is functionality.**

**Clarity is functionality.**

**Context is functionality.**

**Understanding is functionality.**

이 네 가지를 단순한 polish나 디자인 개선 사항으로 취급하지 않는다.

**이것들이 제품 자체다.**
