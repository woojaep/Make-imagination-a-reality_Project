# 왜 이렇게 만들었나 — 네 번째 판

> 비교용 실험판입니다. `main` 은 손대지 않았습니다. 브랜치 `clone-test` · 기준 커밋 `cb6ca0f`

---

## 0. 한 줄

**상자가 상자를 품습니다.** 선을 긋지 않아도 관계가 보이고, 면적을 나눠 가지므로 아무리 커져도 화면을 넘지 않습니다.

---

## 1. 형태를 이렇게 고른 근거

제가 정한 게 아니라 기록에서 나왔습니다.

**① 읽어야 아는 형태는 이미 한 번 거절당했습니다.**
2026-08-21 21:57, AI가 글로 된 3단 구조를 보여줬을 때:

> "3단구조 맞죠? 근데 좀 **난감**하다고 해야 하니요. **직관적이진 않습니다.**
> 만다라트나 조직도, 마인드맵처럼 **도식화나 가시적인 이미지**로 먼저 확인 가능했음 좋겠어요."

→ 제 세 번째 판(토글 목록)이 정확히 그 형태였습니다.

**② "전체 구조가 한눈에 들어오도록"** (2026-08-24 07:48)

**③ `CLAUDE.md` 원칙 15 의 금지 목록** — `Infinite canvas`, `Document manager`
→ 첫 판은 무한 캔버스(네모 37개에 가로 6,948px)로, 세 번째 판은 문서 목록으로 각각 여기에 걸렸습니다.

**셋을 동시에 만족하는 형태가 「담기는 상자」입니다.**

| | |
|---|---|
| 도식이다 | 상자가 상자를 품는다 — 선 없이 관계가 보인다 |
| 한눈에 들어온다 | 면적을 나눠 가지므로 화면을 절대 넘지 않는다 |
| 캔버스도 문서도 아니다 | 좌표도 없고, 위에서 아래로 읽지도 않는다 |

---

## 2. 화면이 하는 일

### 커져도 안 무너진다 — 줄어들되 사라지지 않는다

칸이 좁아지면 **상자 → 한 줄 칩 → 점** 으로 단계적으로 줄어듭니다. 없애는 게 아니라 줄이는 겁니다.

실제 측정입니다.

| 화면 | 상자 | 칩 | 점 | 넘침 |
|---|---|---|---|---|
| 1920×1080 | 37 | 0 | 0 | **0** |
| 1440×900 | 37 | 0 | 0 | **0** |
| 1100×700 | 25 | 12 | 0 | **0** |
| 820×560 | 22 | 3 | 12 | **0** |

첫 판은 같은 내용에서 가로 **6,948px** 였고, 열면 1층 6개 중 **0개**가 보였습니다.

### 읽기 전에 보인다

37개를 한 화면에 놓고 보면, 글자를 읽기 전에 이게 먼저 들어옵니다.

- 「빈틈 찾기」만 **주황**(아직 생각 중)이고 **0/3** — 여섯 중 유일하게 미착수
- 「캔버스 보기」가 **가장 빽빽** — 15개
- 「Claude Code 연결」 안에 주황이 하나 — 결과로 지도 갱신

이게 「조망」입니다. 목록이었으면 위에서 아래로 읽어야 알 수 있는 것들입니다.

### 상태는 셋 — 상상 → 결정 → 현실

```
● 아직 생각 중  →  ● 정했음  →  ● 만들어짐
    (주황)          (파랑)        (초록)
```

본질 문장의 *"무엇이 아직 미정인지 / 무엇을 결정했는지 / 어디까지 만들었는지"* 를 그대로 옮긴 것입니다.
첫 판의 넷을 물려받지 않았고, **「아직 안 정한 것」이라는 별도 목록도 없습니다** — 미정은 색 그 자체입니다.

---

## 3. 손으로 하는 일 — 모드가 없습니다

**보고 있는 그 상자를 그 자리에서 고칩니다. 편집 창이 없습니다.**

| 하고 싶은 것 | 하는 법 |
|---|---|
| 이름 바꾸기 | **글자를 누르면** 바로 고쳐집니다 |
| 상태 바꾸기 | **동그라미를 누르면** 상상 → 결정 → 현실 로 돕니다 |
| 안에 넣기 | **상자를 끌어 다른 상자에 떨굽니다** |
| 밖으로 꺼내기 | **위 주소줄에 떨굽니다** |
| 그 안만 크게 | 상자의 **⤢** |
| 새로 만들기 | 빈 곳을 누르거나 **＋ 더하기** |
| 지우기 | 고르고 `Delete` — **안에 있던 것은 버리지 않고 그 자리로 올라옵니다** |

### 옮겨도 아무것도 끊기지 않습니다

데이터를 **부모 번호를 가리키는 방식이 아니라 통째로 품는 방식**으로 만들었습니다.
그래서 **끊길 선이 애초에 없습니다.**

확인했습니다 — 안에 **15개**가 달린 「캔버스 보기」를 「저장」 안으로 끌어 넣고, 다시 주소줄에 떨궈 꺼냈습니다.
**두 번 모두 15개 그대로**였고, 저장의 진행률은 자동으로 18/19로 갱신됐습니다.

첫 판은 층을 옮기면 **이어둔 선이 전부 끊겼습니다.** 재우가 직접 *"개선해야 할 문제"* 라고 하신 부분입니다.

---

## 4. 앞 판에서 살린 것 / 버린 것

**살린 것** — 통째로 품는 데이터 구조, 모드 없음, 상태 셋, 「적은 글이 곧 첫 뼈대」, 처음 적은 글 보존(대조는 안 함), 첫 판 데이터 읽어오기

**버린 것** — 토글 목록, 들여쓰기, 단축키 여섯 개. 비개발자에게 `Tab`/`Shift+Tab`/`Alt+↑↓` 를 외우게 하는 건 그 자체로 불합격이었습니다

---

## 5. 재우님 데이터는 안 건드렸습니다

첫 판은 `localStorage["sulgyedo.v3"]`, 이 판은 `sangsang4` 를 씁니다.
같은 `file://` 이라 저장 칸을 공유하므로 **첫 판 것은 읽기만 합니다.**
가져올 때 적어두신 「정해진 것」·「아직 안 정한 것」은 하나도 안 버리고 옮깁니다.

**편집·드래그를 다 한 뒤에도 첫 판 네모 37개 그대로**인 것을 확인했습니다.

---

## 6. 확인한 것

화면을 띄워 실제로 끌고 눌러 확인했습니다. **JS 오류 0건.**

- 첫 판 37개 가져오기 → 성공, 원본 보존
- 37개 전부 한 화면 · 가로/세로 넘침 0
- 이름 눌러 고치기 / 동그라미 눌러 상태 바꾸기
- 끌어서 다른 상자 안으로 → **아래 달린 15개 그대로**
- 주소줄에 떨궈 밖으로 → **다시 15개 그대로**
- 자기 안으로는 안 들어감
- ⤢ 로 들어가고 `Esc` 로 나오기
- 1100×700 / 820×560 / 1920×1080 전부 넘침 0

---

## 7. 만들면서 실제로 고친 것 넷

정직하게 남깁니다. 처음 짠 대로는 다 안 됐습니다.

1. **자식 상자가 아예 안 보였습니다** — 부모를 자식보다 나중에 화면에 붙여서 부모가 자식을 덮었습니다
2. **동그라미를 눌러도 상태가 안 바뀌었습니다** — 누르는 순간 화면을 다시 그려서 클릭이 씹혔습니다
3. **이름 위에서는 끌리지가 않았습니다** — 옛 코드에 남아 있던 `stopPropagation` 이 막고 있었습니다.
   상자 머리의 대부분이 이름이라 사실상 **끌 곳이 없는 상태**였습니다
4. **밖으로 꺼낼 방법이 없었습니다** — 상자가 화면을 꽉 채우니 「빈 곳」이 없습니다.
   주소줄에 떨구는 길을 새로 만들었습니다

---

## 8. 이 판이 틀렸을 수 있는 지점

**칸을 균등하게 나눈 것**입니다.

안에 15개가 든 상자와 아무것도 없는 상자가 같은 크기를 받습니다.
빽빽함으로 「어디가 무거운지」는 보이지만, 크기로는 안 보입니다.
내용 양에 비례해 크기를 주는 방법도 있는데, 그러면 작은 항목이 읽기 힘들 만큼 납작해집니다.
**읽히는 쪽을 골랐습니다.** 써보시고 "큰 게 커 보였으면 좋겠다" 싶으면 그때 바꾸면 됩니다.
