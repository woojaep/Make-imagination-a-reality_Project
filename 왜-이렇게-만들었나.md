# 왜 이렇게 만들었나 — 세 번째 판

> 비교용 실험판입니다. `main` 은 손대지 않았습니다. 브랜치 `clone-test` · 기준 커밋 `cb6ca0f`
> 재우가 정리해준 본질만 기준으로 만들었습니다. 첫 판을 따라 만들지 않았습니다.

---

## 0. 한 줄

**구조를 「보는 것」과 「고치는 것」을 한 자리에 붙였습니다.**
보고 있는 그 줄을 그 자리에서 고칩니다. 편집 창이 없습니다.

---

## 1. 본질을 이렇게 읽었습니다

> 아이디어를 눈에 보이는 구조로 정리하고, 그 구조를 보면서 생각하고, 전체를 조망하면서 현실화해간다.

가장 무겁게 받은 문장은 이것입니다.

> **구조화 자체가 생각하는 과정의 일부다.**

그리고 이 루프가 끊기면 안 됩니다.

```
본다 → 생각한다 → 바꾼다 → 다시 본다
```

여기서 세 가지가 나옵니다.

| | 왜 |
|---|---|
| **모드가 없어야 한다** | 루프가 한 덩어리인데 「보기 모드 / 편집 모드」로 나누면 매 바퀴마다 끊긴다 |
| **바꾸는 데 벌칙이 없어야 한다** | 생각은 계속 바뀐다. 바꾸기가 비싸면 사람은 안 바꾸고 머릿속에 담아둔다 |
| **전체가 사라지면 안 된다** | 부분에 있어도 맥락을 잃지 않아야 한다 |

---

## 2. 앞선 두 판이 공통으로 놓쳤던 것

**구조를 고치는 비용이 둘 다 비쌌습니다.**

| | 재구성 비용 |
|---|---|
| **첫 판(재우)** | 층을 옮기면 **이어둔 선이 전부 끊깁니다.** 다시 잇는 건 사람 몫 → 생각을 바꾸는 행동에 **벌칙** |
| **제 두 번째 판** | 더 나빴습니다. **부모를 바꾸는 기능이 아예 없었습니다.** 만들고 지우는 것만 됐습니다 |

둘 다 건물 비유의 여섯 동작 중 **부순다 / 옮긴다 / 재구성한다** 를 못 합니다.

그리고 제 두 번째 판은 **별도 상세 패널을 열어서 고쳤습니다.** 그게 바로 「보기와 편집의 분리」였습니다.

---

## 3. 그래서 만든 것

### ① 모드가 없다

화면에 보이는 모든 줄이 그대로 글자판입니다. 누르면 거기서 바로 고쳐집니다.

- 이름을 누르면 그 자리에서 고쳐짐
- **`Enter`** 새 줄 · **`Tab`** 안으로 · **`Shift+Tab`** 밖으로 · **`Alt+↑↓`** 순서
- **`Ctrl+Enter`** 상태 돌리기 · **`Shift+Enter`** 같은 줄에 생각 적기
- 상태 표시(💭 ✓ ■)를 누르면 바로 바뀜

**창이 열리는 곳은 「처음 적은 글」·「백업」 두 군데뿐**입니다. 구조를 고치는 데는 창이 안 열립니다.

### ② 바꾸는 데 벌칙이 없다

데이터를 **부모 번호를 가리키는 방식이 아니라 통째로 품는 방식**으로 바꿨습니다.
그래서 옮기면 **아래 달린 것이 저절로 따라오고, 끊길 선이 애초에 없습니다.**

실제로 확인했습니다 — 안에 15개가 달린 줄을 `Tab` 으로 옮겨도 **15개 그대로**입니다.
줄을 지워도 안에 있던 것은 버리지 않고 **그 자리로 올라옵니다.**

### ③ 전체가 사라지지 않는다

**들어갔다 나오는 게 아니라 제자리에서 펼쳐집니다.**
그래서 「이 부분이 전체 어디인지」를 주소줄로 알려줄 필요가 없습니다 — 자리가 그대로니까요.

- 열면 **큰 줄기만 보입니다.** 전부 펼쳐놓으면 전체 구성이 안 보입니다
- 접힌 줄도 **안에 몇 개 / 💭 몇 개 / 얼마나 만들어졌는지** 를 옆에 답니다
- 커서가 있는 줄의 **조상 줄이 은은하게 표시**됩니다
- 맨 위에 **💭 ✓ ■ 집계와 전체 진행 눈금**

### ④ 열자마자 커서를 박지 않는다

커서가 있는 줄은 생각까지 펼쳐집니다. 그런데 열자마자 커서를 박으면 그 줄이 글 벽이 되어
**전체 구성이 안 보입니다.** `본다 → 생각한다 → 바꾼다` 순서대로 **보는 게 먼저**입니다.
(만들다가 실제로 겪은 문제라 고쳤습니다)

### ⑤ 상태를 셋으로 다시 뽑았다

첫 판의 넷(생각 중/정해짐/만드는 중/됐음)을 물려받지 않았습니다.
본질 문장에서 **직접** 나오는 건 셋입니다 — *"무엇이 아직 미정인지 / 무엇을 결정했는지 / 어디까지 만들었는지"*.

```
💭 아직 생각 중   →   ✓ 정했음   →   ■ 만들어짐
    상상               결정            현실
```

**「아직 안 정한 것」이라는 별도 목록도 없앴습니다.** 미정은 상태 💭 그 자체입니다.
같은 것을 두 군데에 적게 하지 않습니다.

### ⑥ 적은 글이 그대로 첫 뼈대가 된다

*"뭘 만들고 싶은가요?"* 에 줄바꿈해서 적으면 그게 곧 구조입니다. 들여쓴 줄은 안으로 들어갑니다.
**「밖으로 꺼낸다 → 대략적인 구조로 만든다」를 한 동작으로 붙였습니다.**
첫 판은 여기서 「큰 덩어리 입력」 화면을 한 번 더 거쳤습니다.

### ⑦ 처음 적은 글은 보존하되 대조하지 않는다

`[처음 적은 글]` 을 눌렀을 때만 봅니다. **현재 구조와 자동으로 비교하지 않습니다.**
겹창 안에 이렇게 적어뒀습니다 — *"지금 구조와 달라도 괜찮습니다. 생각은 바뀝니다."*

---

## 4. 안 만든 것 (일부러)

| | 왜 |
|---|---|
| AI 연결 | **발견은 보조 기능**이라고 하셨습니다. 조력자로 붙일 자리는 남겼지만 화면을 차지하게 두지 않았습니다 |
| 원문 ↔ 구조 대조 | 하지 말라고 하신 것입니다. 생각이 바뀌는 건 정상입니다 |
| 자유 배치 · 선 잇기 | 배치를 사람이 하는 건 조망에도 생각에도 기여하지 않습니다. 그리고 그게 첫 판에서 제일 비쌌던 동작입니다 |
| 되돌리기(Ctrl+Z) | 아직 없습니다. **넣어야 할지는 써봐야 압니다** |

---

## 5. 재우님 데이터는 안 건드렸습니다

첫 판은 `localStorage["sulgyedo.v3"]`, 이 판은 `sangsang3` 를 씁니다.
같은 `file://` 이라 저장 칸을 공유하므로 **첫 판 것은 읽기만 합니다.**

가져올 때 **적어두신 「정해진 것」과 「아직 안 정한 것」을 하나도 안 버리고** 생각 칸으로 옮깁니다.
(`· 정한 것` / `? 아직 모르는 것` 표시를 붙여서)

편집한 뒤에도 **첫 판 네모 37개 그대로**인 것을 확인했습니다.

---

## 6. 확인한 것

화면을 띄워 실제로 키보드를 눌러 확인했습니다. **JS 오류 0건.**

- 첫 판 37개 가져오기 → 성공, 원본 보존
- 열자마자 큰 줄기 6줄, 세로 넘침 0, 커서 안 박힘
- `Enter` 새 줄 · `Tab` 안으로 · `Shift+Tab` 밖으로 · `Alt+↑↓` 순서 — 전부 동작
- **안에 15개 달린 줄을 옮겨도 15개 그대로**
- 줄을 지워도 안에 있던 것이 그 자리로 올라옴
- `Ctrl+Enter` 상태 순환 · `Shift+Enter` 생각 적기 — 동작
- 1280×720 / 900×650 / 1920×1080 — 가로 넘침 0

---

## 7. 만들면서 실제로 고친 것 두 개

정직하게 남깁니다. 처음 만든 대로는 본질에 어긋났습니다.

1. **가져온 내용이 전부 펼쳐져 첫 화면이 글 벽이었습니다.** 접어두고 요약만 보이게 고쳤습니다
2. **커서 위치가 뒤집혀 있어** 생각 칸에 글이 앞에 끼어 쓰였습니다. 고쳤습니다

---

## 8. 이 판이 틀렸을 수 있는 지점

**키보드로 구조를 바꾸게 한 것**입니다.

재구성 비용을 가장 낮추는 방법이 키보드라고 판단했습니다. 끌어다 놓기는 첫 판에서
제일 비쌌던 동작이라 뺐습니다. 그런데 재우님은 8/21에 *"조직도를 마인드맵처럼 자유자재로 움직이게끔"*
이라고 하신 적이 있습니다.

**써보시고 손에 안 붙으면 이 판단이 틀린 것입니다.** 그 경우 끌어다 놓기를 더하면 되고,
데이터 구조가 통째로 품는 방식이라 **끌어다 놓기를 더해도 아무것도 안 끊깁니다.**
