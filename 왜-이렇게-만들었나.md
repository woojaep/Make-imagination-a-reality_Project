# 왜 이렇게 만들었나 — 두 번째 판의 판단 근거

> 이 폴더는 **비교용 실험판**입니다. `main` 은 손대지 않았습니다.
> 브랜치 `clone-test` · 기준 커밋 `cb6ca0f`
> 판단 기준은 오직 `CLAUDE.md` 입니다. 첫 판을 따라 만들지 않았습니다.

---

## 0. 한 줄

**첫 판은 「무엇이 있나」를 잘 보여줬고, 이 판은 「무엇을 모르나」와 「지금 어디쯤인가」를 보여줍니다.**

---

## 1. 본질을 무엇으로 읽었나

> **이건 프로젝트 관리 도구가 아니라, 내가 무엇을 아직 모르고 있는지를 눈으로 보게 해주는 도구다.**

`CLAUDE.md` 에서 가장 자주·가장 강하게 반복되는 게 그것이기 때문입니다.

| 원칙 | 문장 |
|---|---|
| 8 | IDEA / DECIDED / UNDECIDED 를 구분한다. **AI가 추론한 것을 사용자 결정으로 저장하지 않는다** |
| 9 | AI-generated 와 User-confirmed 를 구분한다 |
| 10 | AI의 핵심 작업은 **빈틈을 발견하도록 돕는 것** |
| 11 | **미정은 오류가 아니다** |
| 16-5 | 결정된 것과 미정인 것이 구분되는가 |

지도·층·네모·줌은 전부 **원칙 18이 말한 "표현 예시"** 로 읽었습니다.
그래서 그 표현들을 지킬 의무가 없다고 판단했습니다.

---

## 2. 가장 크게 어긋난 것 — 무한 캔버스

첫 판의 문제 4개(인수인계 §8)는 **증상이고, 원인은 하나**입니다.

**원칙 15의 금지 목록에 `Infinite canvas` 가 명시되어 있는데, 첫 판이 정확히 그것이었습니다.**

측정한 값입니다.

| | 첫 판 | 이 판 |
|---|---|---|
| 네모 37개일 때 가로 폭 | **6,948px** | **화면 폭 그대로 (넘침 0)** |
| 열었을 때 보이는 1층 | **0개 / 6개** | **6개 / 6개** |
| 2층까지 동시에 보임 | 안 됨 | **보임** |
| 「아직 모르는 것」 13개 위치 | 숫자만. 하나씩 열어봐야 앎 | **오른쪽에 항상 펼쳐져 있음** |
| 1024·820·1920 폭에서 가로 스크롤 | 발생 | **0 (전부 확인)** |

캔버스는 **네모가 늘수록 조망이 나빠집니다.** North Star 가 "내려다본다"인데 정확히 반대로 동작합니다.
그래서 캔버스를 고치지 않고 버렸습니다.

---

## 3. 바꾼 것과 그 근거

### ① 배치를 사람이 하지 않는다 — 자유 배치·좌표 저장 삭제

**근거: 원칙 17.** 새 기능은 셋 중 하나에 기여해야 합니다 — 생각을 더 잘 보이게 / 빈틈을 줄이게 / 전체와 부분의 관계를 이해하게.
**네모를 예쁘게 놓는 일은 셋 중 어디에도 기여하지 않습니다.** 그건 그림 그리기지 생각 정리가 아닙니다.

개수를 보고 칸 수와 빽빽함을 프로그램이 정합니다. 좁아지면 설명 → 곁가지 순으로 접습니다(원칙 4).

> 잃은 것: 자유 배치, 선 잇기·끊기, 범위 선택, 자동 정리, 되돌리기.
> **첫 판 기능 15개 중 9개를 버렸습니다.** 조망을 지키는 값으로 치렀습니다.

### ② 「아직 모르는 것」을 오른쪽에 못 박았다

**근거: 원칙 11 + 16-5.** 미정이 이 제품의 중심인데 첫 판에서는 가장 안 보이는 자리에 있었습니다.
이제 **화면에서 사라지지 않습니다.** 어느 요소 것인지 이름이 같이 붙고, 누르면 그리로 갑니다.
띠 밑에 *"미정은 오류가 아닙니다"* 라고 적어뒀습니다 — 원칙 11의 문장 그대로입니다.

### ③ 한 단계 아래 이름을 카드 안에 같이 보여준다

**근거: 원칙 5 (Focus without losing context) + 13 (구조와 진행을 같이).**
남는 자리를 빈 칸으로 두는 대신 **누르지 않고도 두 층을 같이 보게** 했습니다.
좁아지면 이 곁가지부터 접습니다. 정보를 늘린 게 아니라 **관계를 보이게** 한 것입니다(원칙 4).

### ④ 층(1층·2층·3층)이라는 이름을 없앴다

**근거: 원칙 6.** 흐름형을 골라도 "층"이라 부르면 구조를 하나로 강제하는 것입니다.
이제 「영역 / 흐름」을 고르면 **같은 데이터를 다르게 배열**합니다 — 흐름이면 번호가 붙고, 영역이면 격자입니다.
데이터에는 `lv` 가 아예 없습니다. 깊이는 부모를 따라가서 계산합니다.

### ⑤ 「아이디어 → 큰 덩어리 입력」 화면을 없앴다

**근거: 원칙 7.** 처음부터 완벽한 지도를 만들지 않습니다.
빈 지도로 시작해서 AI에게 묻거나 직접 채우는 게 **단계가 하나 적습니다.**
적어둔 것은 하나도 안 버립니다 — 「추가로 생각난 것」은 요소로, 「아직 모르는 것」은 미정으로 그대로 옮깁니다(원칙 8).

### ⑥ AI 왕복의 「돌아오는 방향」을 만들었다

**근거: 원칙 10 — 이 도구의 존재 이유.** 첫 판에는 나가는 방향(작업 메모 복사)만 있었습니다.

- **[AI에게 물어볼 것]** — 지금 보는 범위의 구조·정한 것·모르는 것을 글로 만들어 복사합니다.
  그 글 안에 규칙을 박아뒀습니다: *"질문은 최대 3개"*(원칙 11), *"분명히 정하지 않은 것을 정해진 것처럼 쓰지 마라"*(원칙 8), *"이미 적어둔 것과 겹치는 질문은 하지 마라"*.
- **[AI 답변 붙여넣기]** — `질문:` `제안:` `설명:` 세 줄 형식만 읽습니다.
  **`제안:` 으로 들어온 것은 `✨` 점선 테두리가 붙고, 내가 [내 것으로] 를 누르기 전까지 내 것이 아닙니다**(원칙 9).
  `질문:` 은 「아직 모르는 것」으로만 들어갑니다. **AI가 「정해진 것」에 쓰는 길은 코드에 없습니다**(원칙 8).

**열쇠를 HTML 에 박는 방식은 쓰지 않았습니다.** 유출되고, 한도를 「차단」이 아니라 「알림」으로밖에 못 겁니다.

### ⑦ 다시 열면 지난번 보던 자리로 간다

**근거: 원칙 14.** 첫 판에 없던 것입니다. 맨 위로 되돌리면 지난번 자리를 매번 다시 찾아 들어가야 합니다.
단, **들여다본 것만으로는 「마지막 작업 시각」이 안 바뀝니다.** 고친 게 아니니까요.

### ⑧ 집 화면 카드에서 정보를 3개 줄였다

**근거: 원칙 4 (More information ≠ Better UI).** 첫 판은 상태·%·❓·네모수·시각 다섯 개였습니다.
**「어디까지 왔나」와 「무엇을 모르나」 둘만 남겼습니다.**

---

## 4. 안 바꾼 것 (일부러)

| | 왜 |
|---|---|
| 상태 4단계와 그 색 | 재우가 이미 눈에 익힌 것입니다. 바꿀 근거가 문서에 없습니다 |
| 밝은 테마 | 확정된 설계 결정입니다 |
| 아이디어 = 쪽지 / 프로젝트 = 지도 | 원칙 12 그대로입니다 |
| 백업 복사·불러오기 | 저장이 브라우저에만 있는 한 필요합니다 |
| 「정해진 것 / 아직 모르는 것」 두 칸 구조 | 원칙 8의 핵심입니다. 첫 판이 이미 맞게 했습니다 |

---

## 5. 재우님 데이터는 안 건드렸습니다

첫 판은 `localStorage["sulgyedo.v3"]` 를 씁니다. 같은 `file://` 이라 **저장 칸을 공유합니다.**
여기서 그 칸에 쓰면 원본이 덮입니다.

그래서 이 판은 **자기 칸(`sangsang.v1`)만 쓰고, 첫 판 것은 읽기만 합니다.**
처음 열면 첫 판 내용을 자동으로 복사해옵니다. **두 판을 나란히 켜놓고 비교할 수 있습니다.**

확인했습니다 — 새 판을 열고 편집한 뒤에도 첫 판 네모 수는 **37개 그대로**입니다.

---

## 6. 확인한 것

띄워서 실제로 눌러본 결과입니다. (스크립트로 부른 게 아니라 화면을 띄워 클릭했습니다)

- JS 오류 **0건**
- 첫 판 37개 네모 가져오기 → **성공**, 원본 **보존됨**
- 전체 보기 / 15개짜리 안으로 들어가기 / 흐름·영역 전환 / Esc 로 나가기 — **가로 넘침 0**
- 1024×700 · 820×620 · 1920×1080 — **전부 가로 넘침 0**
- 상세에서 「아직 모르는 것」 추가 → **오른쪽 띠에 즉시 반영**
- AI 답변 붙여넣기 → 질문 1개는 미정으로, 제안 1개는 `✨` 로 들어감. **이미 설명이 있는 요소는 덮어쓰지 않음**
- 다시 열었을 때 지난번 자리 복원 — **동작**

---

## 7. 아직 안 한 것 (정직하게)

- **AI가 실제로 쓸 만한 질문을 뽑는지는 검증 안 됐습니다.** 왕복 길만 만들었습니다.
  이건 재우님이 아이디어 하나를 실제로 던져봐야 압니다. **이 프로젝트 최대 미검증 요소 그대로입니다**
- 부분 생성(영역 안만 AI로 채우기) — 왕복이 이미 「지금 보는 범위」 기준이라 자리는 잡혀 있습니다
- 검색 — 프로젝트가 많아지면 필요합니다
- 되돌리기(Ctrl+Z) — 첫 판에 있었는데 안 넣었습니다. **지우기에만 확인창을 뒀습니다.** 넣어야 할지는 써봐야 압니다

---

## 8. 이 판이 틀렸을 수 있는 지점

가장 크게 건 판단은 **「자유 배치를 버린 것」** 입니다.

재우님은 8/21에 *"조직도를 마인드맵처럼 자유자재로 움직이게끔"*, *"전부 한 화면에 펼쳐주세요"* 라고
직접 요구하셨습니다. 그런데 나중에 쓰신 `CLAUDE.md` 원칙 15·17·18은 그것과 어긋납니다.

**저는 `CLAUDE.md` 를 골랐습니다.** 그게 이번 지시였고, 나중에 쓰신 문서이기 때문입니다.
**틀렸다면 이 판단이 틀린 것입니다.** 그 경우 되돌릴 곳은 딱 하나 — 배치 기능이고, `main` 에 그대로 있습니다.
