# 「상상을 현실로」 진행 현황 — 인수인계 문서

> 다른 AI/사람에게 이 프로젝트를 넘길 때 이 문서 하나만 주면 됩니다.
> 마지막 갱신: 2026-08-22

---

## 0. 한 줄 요약

머릿속 아이디어를 **눈에 보이는 구조**로 만들고, 그 생각이 **현실의 결과물이 되어가는 과정을 조망**하게 하는 개인용 도구.
현재 **HTML 파일 하나**로 돌아가는 웹앱. 비개발자가 바이브코딩으로 만들고 있음.

**North Star** — 상상을 보이게 하고, 현실화 과정을 내려다본다.
(`내려다본다`는 비유. 특정 UI(트리·마인드맵·무한캔버스)를 뜻하지 않음)

**핵심 원칙 5줄**
1. 복잡한 것을 더 복잡하게 보여주지 않는다. 한눈에 이해되게 만든다.
2. Visibility / Clarity / Context / Understanding 은 폴리시가 아니라 **기능 자체**.
3. AI가 추측한 것을 **사용자 결정으로 저장하지 않는다**.
4. **미정은 오류가 아니다.** 모르면 모르는 채로 남긴다.
5. 기능을 더 추가하기 전에 **먼저 단순화**한다.

전체 원칙은 같은 폴더의 `CLAUDE.md` 참고.

---

## 1. 지금 어디까지 왔나

| 영역 | 상태 |
|---|---|
| 집 화면 (아이디어 + 프로젝트 목록) | ✅ 완료 |
| 아이디어 쪽지 화면 | ✅ 완료 |
| 아이디어 → 첫 지도 만들기 | ✅ 완료 (AI 없이, 사람이 큰 덩어리 입력) |
| 프로젝트 지도 (캔버스) | ✅ 완료 |
| 요소 상세 패널 | ✅ 완료 |
| 밝은 테마 | ✅ 완료 |
| **AI 연결** | 🌱 **미착수 — 이 프로젝트 최대 미검증 요소** |
| 부분 생성 (영역에 들어가서 그 안만 채우기) | 🌱 미착수 |
| 구조 질문 3개 (누가 쓰나 / 어디서 / 입출력) | 🌱 미착수 |

**가장 큰 리스크**: 「AI가 진짜 쓸모 있는 빈틈을 찾아주는가」가 **한 번도 검증되지 않음.**
이게 안 되면 나머지가 다 예뻐도 이 도구는 껍데기가 됨.

---

## 2. 화면 구조 (4개)

```
① 집 (Home)
   ├ 맨 위: "지금 뭐가 떠올랐나요?" 입력칸 → 아이디어 생성
   ├ 💡 아이디어 목록 (접어둔 것은 흐리게 아래로)
   ├ 📁 프로젝트 목록 (상태별 그룹) + [＋ 빈 프로젝트] [기존 프로젝트 복사]
   └ 📦 보관함
        ↓ 아이디어 클릭            ↓ 프로젝트 카드 클릭
② 아이디어 쪽지                ③ 프로젝트 지도 (캔버스)
   제목 / 처음 적은 것            층 띠 + 자유 배치 + 선
   추가로 생각난 것(쌓기)              ↓ 이름 클릭 or ⋯
   ❓ 아직 안 정한 것             ④ 요소 상세 (오른쪽 패널)
   [접어두기] [프로젝트로 만들기]
        ↓
   ②-b 만들기 화면 (한 페이지)
   원본 보기 → 흐름/영역 고르기 → 큰 덩어리 입력 → 실시간 미리보기 → 생성
```

주소줄: `🏠 집 › 프로젝트 이름 › 영역 › 세부`

---

## 3. 만들어진 기능 전체

### 집
- 아이디어 담기 (Ctrl+Enter), 목록, 삭제
- 프로젝트 카드: 이름 / 설명 / 진행률 막대 / 상태 / ❓개수 / 네모수 / 마지막 작업 시각
- 카드 `⋯` 메뉴: 상태 4단계 변경, 보관함 넣기·꺼내기, 이름 바꾸기, 지우기
- `＋ 빈 프로젝트`, `기존 프로젝트 복사`(구조 + 아직 안 정한 것만 가져오고 진행 상태는 초기화)
- 이전 저장분 자동 이전 (v1 나무구조 → v2 단일 프로젝트 → v3 다중)

### 아이디어 쪽지
- 제목(자동 생성 + 편집), 처음 적은 것, 추가로 생각난 것(시각 표시하며 한 줄씩 쌓임), ❓ 아직 안 정한 것
- 접어두기 / 다시 펴기 (지우지 않고 남김)
- 프로젝트로 만들기 → 만들기 화면
- AI 대화 자리는 회색 칸으로 표시만 (미구현)

### 만들기 화면 (아이디어 → 첫 지도)
- 원본(처음 적은 것 / 추가 / ❓) 계속 보임
- 나누는 방식: **순서대로(흐름)** / **종류별로(영역)** 선택
- 큰 덩어리 직접 입력 + `✨ 뼈대 제안받기`(기본 틀 넣어줌, 고치면 ✨ 해제)
- 실시간 미리보기 → 생성
- 생성 규칙:
  - 1층 = 큰 덩어리, 전부 🌱 생각 중
  - **추가로 생각난 것 → 「자리 미정」 띠에 네모로** (결정이 아니므로 「정해진 것」에 안 넣음)
  - **❓ → 「아직 안 정한 것」이라는 자리 미정 네모 하나에 모아서** (버리지 않음)
  - 제안받은 네모는 `✨` 표시, **열어보면 사라짐**
  - 아이디어는 지워지지 않고 남고, 프로젝트 툴바에 `💡 처음 아이디어` 버튼 생김

### 프로젝트 지도 (캔버스)
- 가로 띠로 층 구분 (1층·2층·3층…, 맨 아래 「자리 미정」)
- **자유 배치** — 어디든 끌어다 놓기, 놓은 자리 저장
- **승격/강등** — 다른 층으로 옮기면 이어진 선이 전부 끊김 (다른 네모는 안 건드림)
- **선 잇기** — 네모 아래 `●` 끌기(방향 무관, 위층이 부모) 또는 두 네모 `Shift+클릭`
- **선 끊기** — 선 위 `✕`, 선 클릭, Shift+클릭 토글, 상세의 「붙은 선 전부 끊기」
- **층마다 다른 네모 모양** — 1층 크고 굵고 설명까지 / 2층 카드 / 3층 작게 / 4층+ 흐리게. 왼쪽 세로 띠 굵기도 층마다 다르고 **색은 상태 색**
- **⤢ 안으로 들어가기** (또는 더블클릭) — 그 네모를 크게 보고 그 아래만 표시. **층 번호는 그대로 유지**, 크기만 커짐. 주소줄·형제 이동(‹ ›)·Esc로 나가기
- 들어간 화면에서 선이 끊겨도 **네모는 그 자리에 남음** (`keepIn`)
- **범위 선택** — 빈 곳 드래그(닿으면 선택), Ctrl+클릭 추가, Ctrl+A 전체
- **여러 개** — Delete로 지우기(선택한 것만, 아래는 남고 선만 끊김), 같이 옮기기(같은 층 안에서만)
- 자동 정리 / 접기·펴기 / 가지 강조(하나 선택 시 나머지 흐리게) / 되돌리기 Ctrl+Z
- 전체 글로 복사, 백업 복사·불러오기

### 요소 상세 패널
- 이름 / 상태 4단계 / 「이게 뭔가」 / 「정해진 것」 / 「아직 안 정한 것」 / 아래에 붙은 것 목록 + 추가
- **작업 메모 복사** — 그 네모의 맥락(프로그램 설명, 위치, 정해진 규칙, 안 정한 것, 아래 항목)을 글로 만들어 복사 → Claude Code에 붙여넣는 용도
- ⤢ 안으로 들어가기 / 붙은 선 전부 끊기 / 지우기

---

## 4. 확정된 설계 결정

| | 결정 |
|---|---|
| 상태 체계 | 🌱 생각 중 / 📋 정해짐 / 🔨 만드는 중 / ✅ 됐음 — **네모와 프로젝트가 같은 4개를 씀** |
| 보관 | 상태가 **아님**. 별도 스위치. 어떤 상태의 프로젝트든 보관 가능 |
| 아이디어 상태 | **없음**. 결말은 셋뿐 — 그대로 둠 / 프로젝트가 됨 / 접어둠 |
| 아이디어 vs 프로젝트 | 아이디어 = **쪽지(글+대화)**, 프로젝트 = **지도**. 아이디어에는 지도 없음 |
| 구조 방식 | **흐름형/영역형을 강제하지 않음.** 프로젝트마다 더 이해되는 쪽으로 |
| 층 이동 | 선이 끊김. 다른 네모는 안 건드림. 같은 층 안 이동은 아무것도 안 끊김 |
| 「정해진 것」 | **사용자가 명시적으로 결정한 것만.** AI 추측 절대 금지 |
| 「추가로 생각난 것」 | 결정이 아님 → 「자리 미정」으로 감 |
| AI 제안 표시 | `✨`, 사용자가 열어보면 사라짐 |
| 첫 지도 | **큰 그림(1층)만.** 세부는 들어가서 부분 생성 |
| 질문 | 최대 3개, 객관식, **「아직 모르겠어요」가 정상 답** → ❓로 쌓임 |
| 저장 | 브라우저 localStorage + 백업 복사·붙여넣기 |
| 테마 | 밝은 테마 고정 (다크모드 제거) |
| 이름 | 캔버스 아래 띠 = 「자리 미정」 / 프로젝트 선반 = 「보관함」 / 접은 아이디어 = 「접어둠」 |

---

## 5. 데이터 구조

`localStorage["sulgyedo.v3"]`

```js
APP = {
  ideas: [{
    id, title, text,
    notes: [{ t, at }],        // 추가로 생각난 것
    open: [],                  // 아직 안 정한 것
    createdAt, updatedAt,
    done: null | "project" | "shelved",
    projectId
  }],
  projects: [{
    id, title, what,
    status: "idea"|"set"|"doing"|"done",
    archived: false,
    createdAt, updatedAt,
    fromIdea,                  // 어느 아이디어에서 왔나
    way: "flow" | "area",      // 나누는 방식
    nodes: [{
      id, name,
      status: "idea"|"set"|"doing"|"done",
      what,                    // 이게 뭔가
      decided: [],             // 정해진 것 (사용자 말만)
      open: [],                // 아직 안 정한 것
      parent: null | nodeId,
      lv: 0|1|2|3...,          // 0 = 자리 미정 띠
      x, y,                    // 캔버스 좌표 (y는 lv 띠 기준 절대값)
      fold: bool,
      ai: true?,               // ✨ 제안받은 것 (열어보면 삭제됨)
      keepIn: viewId?          // 선 끊겨도 그 화면에 머물게
    }]
  }]
}
```

이전 키: `sulgyedo.v2`(프로젝트 하나), `sulgyedo.v1`(kids 나무구조) — 자동 이전 코드 있음.

**주요 전역 변수**
`APP` / `DOC`(지금 연 프로젝트) / `IDEA`(지금 연 쪽지) / `MK`(만들기 화면 상태) / `VIEW`(들어와 있는 네모 id) / `SEL`(선택된 네모들) / `UNDO` / `OFF`(화면 위로 당긴 픽셀)

**화면 전환**: `document.body` 클래스 — `athome` / `atidea` / `atmake` / (없으면 프로젝트 지도)

**주요 상수**: `BAND_H=200`, `NW(lv)` = 238/196/172/154, `PAD_X=76`, `PAD_Y=44`, `MIN_LV=3`, `GAP=18`, `RAIL=58`

---

## 6. 아직 안 만든 것

| | 내용 |
|---|---|
| **AI 연결** | 방식 미결정 (복사·붙여넣기 왕복 vs API 열쇠). **최우선 미검증** |
| **부분 생성** | 영역에 들어가서 "이 안을 더 짜줘" |
| **구조 질문 3개** | 누가 쓰나 / 어디서 돌아가나 / 뭘 넣으면 뭐가 나오나 |
| 복사 세부 옵션 | 지금은 고정(구조 + ❓). 체크박스로 고르게 하기 |
| 안전장치 3개 | 끊기기 전 미리 알림 / 「방금 끊긴 선 다시 잇기」 / 안 이어진 네모 표시 |
| 가지째 옮기기 | Shift+드래그로 자식까지 |
| 부모 둘 이상 | 한 요소가 두 영역에 걸치는 경우 |
| 검색 | 프로젝트가 많아지면 필수 |
| Claude Code 결과 붙여넣어 지도 갱신 | 지금은 나가는 방향(작업 메모 복사)만 있음 |
| 틀(템플릿)로 저장 | 잘 만든 프로젝트를 틀로 |

---

## 7. 아직 안 정한 것

- **AI를 어떻게 부를까** — 복사·붙여넣기 왕복 / API 열쇠 / 다른 방법
- **층 이름표** — 흐름형인데 "층"이라고 부르는 게 맞나? "단계"가 맞나?
- **화면별 좌표 분리** — 지금은 전체 보기와 들어간 화면이 좌표를 공유
- **1층 네모 → 별도 프로젝트로 승격** 기능이 필요해질 시점
- 아이디어가 많아졌을 때 접기·검색

---

## 8. 알려진 원칙 위반 (고쳐야 할 것)

`CLAUDE.md` 기준으로 스스로 점검한 결과:

1. **「1층/2층/3층」이 구조를 하나로 강제함** (원칙 6 위반)
   → 흐름형을 골라도 "층"이라 부름. 흐름이면 "단계"가 맞음. 방식에 따라 띠 이름이 바뀌어야 함
2. **전체 보기에서 "지금 어디"가 안 보임** (원칙 16-2)
   → 주소줄이 들어갔을 때만 뜸. `Focus without losing context` 를 반쪽만 지킴
3. **「정해진 것」이 네모 겉면에서 안 보임** (원칙 16-5)
   → 겉에는 ❓개수만. 결정/미정 구분이 이 제품의 핵심인데 반만 보임
4. **집 화면 카드 정보 과다** (원칙 4)
   → 상태·%·❓·네모수·시각 5개. More information ≠ Better UI

---

## 9. 다음에 할 일 (권장 순서)

1. **AI 연결 방식 결정** — 이거 없이는 핵심이 안 움직임
2. **코드 없이 검증** — 아무 아이디어나 던져서 AI가 뽑는 질문/구조가 쓸모 있는지 먼저 확인
3. **만들기 화면의 「큰 덩어리」 칸을 AI가 채우게** — 화면·흐름은 그대로, 채우는 주체만 바뀜
4. **부분 생성** — 영역에 들어가서 그 안만
5. 위 8번의 원칙 위반 4개 정리

---

## 10. 기술 환경

- **파일 하나**: `설계도.html` (약 120KB, HTML+CSS+JS 전부 한 파일, 외부 라이브러리 없음)
- 폰트만 Google Fonts (IBM Plex Sans KR / IBM Plex Mono)
- 여는 법: 파일 더블클릭 또는 `설계도 열기.bat`
- 웹 주소로도 배포되어 있음 (Claude Artifact)
- 저장소: `github.com/woojaep/Make-imagination-a-reality_Project` (private, main)
- 커밋 이력:
  - `e8cbed4` 설계도 캔버스 첫 버전
  - `42ed7a5` 층별 네모 모양 + 범위 선택 + 여러 개 지우기·옮기기
  - `8157ad9` 집 화면 · 아이디어 쪽지 · 첫 지도 만들기 · 밝은 테마
- 같은 폴더 파일: `CLAUDE.md`(제품 원칙), `현황-인수인계.md`(이 문서)

**주의**: 앱 내용은 **브라우저에 저장**됩니다. 저장소에는 프로그램만 있고 적어둔 내용은 없습니다. 백업하려면 앱에서 「백업 복사」.
