# B안 — 설계 메모

> A/B 테스트용 B안입니다. `main`(A안)은 손대지 않았습니다.
> 브랜치 `clone-test` · 기준 커밋 `cb6ca0f`

---

## 무엇을 만들었나

**조직도 + 마인드맵.** 카드를 원하는 자리에 자유롭게 놓고, 원하는 대로 잇고, 상하위를 자유롭게 바꿉니다.
그러면서도 **전체 지도는 늘 한 화면에** 들어옵니다.

## 이걸 가능하게 하는 장치는 하나입니다

> **지도가 화면에 맞춰 옵니다.**

카드를 어디에 놓든, 몇 개가 되든 화면은 항상 전체를 담습니다. 스크롤이 없습니다.
자유 배치를 마음껏 해도 조망이 안 깨지는 이유가 이겁니다.

세부가 필요하면 **`Ctrl + 휠`로 확대**합니다. **`전체 보기`** 또는 **`Esc`** 로 언제든 돌아옵니다.

## 조작 — 열두 개, 전부 한 동작

| | |
|---|---|
| 새로 만들기 | 빈 곳 **두 번 클릭** → 바로 이름 입력 |
| 이름·설명 고치기 | 글자 **클릭** |
| 옮기기 | 카드 **끌기** — 어디든 |
| 잇기 | 카드 아래 **동그라미를 끌어** 다른 카드에 놓기 |
| 상하위 바꾸기 | 카드를 다른 카드에 **겹쳐 놓기** |
| 끊기 | 선 위 **✕** |
| 상태 바꾸기 | 카드의 **점 클릭** (생각 중 → 정했음 → 만들어짐) |
| 지우기 | 고르고 **Delete** |
| 정렬 | **⤢ 정렬** |
| 되돌리기 | **Ctrl+Z** |
| 확대·축소 | **Ctrl + 휠** (커서 자리 기준) |
| 전체로 | **전체 보기** 또는 **Esc** |

**끄는 것과 잇는 것을 나눴습니다.** 카드를 끌면 자리만 바뀌고 선은 그대로, 동그라미를 끌면 자리는 그대로고 선만 이어집니다.

## 멀어지면 줄이되 없애지 않습니다

| 카드 크기 | 보이는 것 |
|---|---|
| 넉넉 | 이름 + 설명 |
| 좁음 | 이름만 |
| 아주 작음 | **색과 선만** (마우스 올리면 이름) |

**색과 선은 아무리 작아져도 안 없앱니다.** 그 둘이 전체 보기의 정보 전부입니다.

## ⤢ 정렬

누를 때만 돕니다. 평소엔 놓으신 자리 그대로입니다.
**`Ctrl+Z` 한 번이면 정렬 전 배치로 돌아옵니다.**

정렬은 화면 비율에 맞춰 **줄 수를 여러 개 시험해보고 가장 크게 보이는 배치**를 고릅니다.
만들면서 이걸 네 번 고쳤습니다:

| | 배율 | 문제 |
|---|---|---|
| 처음 | **21%** | 자식 15개를 한 줄로 늘어놔서 폭이 3,000px |
| 자식 묶음 배치 | 32% | 뿌리 6개를 한 줄로 늘어놔서 4,184px |
| 뿌리 줄바꿈 | 55% | 화면이 가로로 긴데 세로로 길게 정렬 |
| **줄 수 자동 선택** | **70%** | — 설명까지 읽히는 단계 |

## 확대 중에만 미니맵

오른쪽 아래에 작게. **전체 보기에서는 안 뜹니다.**
지금 보고 있는 범위가 네모로 표시되고, 미니맵을 누르면 그 자리로 갑니다.

## 재우님 데이터는 안 건드렸습니다

A안은 `localStorage["sulgyedo.v3"]`, B안은 `sangsangB` 를 씁니다.
같은 `file://` 이라 저장 칸을 공유하므로 **A안 것은 읽기만 합니다.**
가져올 때 「정해진 것」·「아직 안 정한 것」은 하나도 안 버리고 카드 설명으로 옮깁니다.

**끌고 잇고 지운 뒤에도 A안 네모 37개 그대로**인 것을 확인했습니다.

## 확인한 것

실제로 끌고 눌러 확인했습니다. **JS 오류 0건.**

- A안 37개 가져오기 → 성공, 원본 보존
- 전체 보기 70%, 이름·설명 읽힘, 37개 + 선 31개 전부 한 화면
- 카드 끌어 이동 → **부모는 그대로**
- 겹쳐 놓기 → 상하위 바뀜, **원래 부모의 나머지 자식은 그대로**
- 동그라미 끌기 → **자리는 그대로, 선만 이어짐**
- 순환 연결(자기 조상에 잇기) 안 됨
- 선 ✕ 로 끊기
- `Ctrl+휠` 확대 139% → 미니맵 뜸 / `전체 보기` → 미니맵 숨음
- 빈 곳 두 번 클릭 → 새 카드 + 바로 입력
- `Ctrl+Z` 로 이동·연결·정렬 전부 복원
- 1200×760 / 900×640 / 1920×1080 넘침 0

## 만들면서 실제로 고친 것

1. **버튼 세 개가 겹쳐 보였습니다** — 끌기 미리보기 선 클래스를 `.ghost` 로 지었는데 버튼의 `.btn.ghost` 와 이름이 겹쳐 버튼들이 절대 위치가 됐습니다
2. **잇기 동그라미가 안 눌렸습니다** — 카드에 `overflow:hidden` 이 걸려 있어 밖으로 나온 동그라미가 잘렸습니다
3. **가져온 직후 정렬이 정사각형이 됐습니다** — 그 시점엔 지도 화면이 아직 안 열려 있어 화면 크기가 0으로 잡혔습니다
4. **정렬 배율 21% → 70%** (위 표)

## 아직 없는 것

- **AI** — 넣지 않았습니다. 조력자로 붙일 자리는 남아 있습니다
- **여러 부모** — 한 카드는 부모 하나입니다. 필요해지면 곁줄로 넣습니다
- **가지 접기** — 카드가 아주 많아지면(200개쯤) 축소만으로는 한계가 옵니다. 지금 37개에서는 필요 없어 안 넣었습니다
- **선의 종류**(상하위 / 관련 / 흐름) — 지금은 「이어짐」 하나뿐입니다
